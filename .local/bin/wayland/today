#!/bin/sh

today=$(date -I)

[ -z "$@" ] || [ "$@" = "copy" ] &&
    {
        wl-copy $today
        notify-send $today "copied to clipboard"
    }

[ -z "$@" ] || [ "$@" = "put" ] &&
    {
        # broken! kills wayland?!
        # wtype $today
        notify-send $today "putting broken"
    }
